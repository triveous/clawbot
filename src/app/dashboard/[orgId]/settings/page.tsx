import { EmptyStage } from "@/components/dashboard/empty-stage";

export default function SettingsPage() {
  return (
    <EmptyStage icon="settings" title="Settings" stage="Stage 4">
      Account, organization, and notification preferences will live here. The design is built
      — wiring lands in the next stage.
    </EmptyStage>
  );
}
