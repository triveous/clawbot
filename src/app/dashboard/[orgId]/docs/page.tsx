import { EmptyStage } from "@/components/dashboard/empty-stage";

export default function DocsPage() {
  return (
    <EmptyStage icon="bookOpen" title="Docs" stage="Stage 4">
      In-dashboard docs: quickstart, CLI reference, SDK, security, access modes, versions.
    </EmptyStage>
  );
}
