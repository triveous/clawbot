import { EmptyStage } from "@/components/dashboard/empty-stage";

export default function NotificationsPage() {
  return (
    <EmptyStage icon="bell" title="Notifications" stage="Stage 4">
      Full inbox view with filters, bulk actions, and persisted read state.
    </EmptyStage>
  );
}
